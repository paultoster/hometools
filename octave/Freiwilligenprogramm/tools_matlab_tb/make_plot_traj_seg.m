function [seg,nseg,q] = make_plot_traj_seg(q)
%
%

  
  %===================================================================
  % Set / Prood Parameter
  %===================================================================  
  q = make_plot_traj_seg_param(q);

  %===================================================================
  % Segmente vorbereiten
  %===================================================================  
  [seg,nseg,q]    = make_plot_traj_seg_calc(q);
  
end
  